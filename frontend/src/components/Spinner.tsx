const Spinner = () => (
  <div className="flex items-center justify-center w-full h-full min-h-[60vh]">
    <div className="h-8 w-8 animate-spin rounded-full border-[3px] border-foreground/10 border-t-foreground" />
  </div>
);

export default Spinner;
